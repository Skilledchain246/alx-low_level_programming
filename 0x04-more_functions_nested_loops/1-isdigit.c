#include "main.h"

/**
 * _isdigit - function that checks for a digit
 * @c: int type
 * Return: 1 if gitgits or 0
 */
int _isdigit(int c)
{
	if (c > 47 && c < 58)
		return (1);
	else
		return (0);
}
